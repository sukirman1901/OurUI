# Tutorial 04 — Routing

This step adds multiple pages in one module. The runnable source is **`examples/tutorial/04_routing.py`** — it defines `home` and `about` as separate `ui.Page` instances with `route="/"` and `route="/about"`.

Full walkthrough (goal, code explanation, and CLI notes) is in the next docs pass. For now, serve the file and visit `/` and `/about`:

```bash
ourui serve examples/tutorial/04_routing.py
```

## Next

- [Tutorial 05 — Theme and tokens](05-theme-tokens.md)
